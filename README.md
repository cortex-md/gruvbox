# Gruvbox for Cortex

Vault-scoped Cortex theme package with dark and light variants.

Install by copying this directory to:

```text
<vault>/.cortex/themes/gruvbox
```

The manifest exposes one theme family named `gruvbox`. Cortex loads `dark.css` or `light.css`
on demand according to the active color scheme.
